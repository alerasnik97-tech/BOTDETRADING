# NEXT PROMPT — DATA COMPLETENESS RECOVERY

**Priority: owner action. Blocks lab (Phase E B4).**

## Confirmed missing (existence-checked, no data loaded)
| Asset | Canonical path | Needed by |
|---|---|---|
| `forex_factory_cache.csv` | `05_MARKET_DATA_VAULT/data/forex_factory_cache.csv` | `build_usdjpy_news_fortress_dataset.py`, `news_filter.py`, news_phase3, `test_usdjpy_readiness` |
| `news_eurusd_v2_utc.csv` | `05_MARKET_DATA_VAULT/data/news_eurusd_v2_utc.csv` | `build_am_grade_news_dataset.py`, config `DEFAULT_NEWS_V2_UTC_FILE` |
| high-precision M1 dukascopy bundle | `05_MARKET_DATA_VAULT/legacy_data/data_precision/dukascopy` | high-precision data loader |

Present but **provenance UNVERIFIED**: `canonical_anchor_events.csv`
(168 rows, untracked+gitignored, regenerated by a net/2025-26-capable
official-anchor pipeline outside Phase D scope). Treat as **not lab-trustworthy
until audited**; do **not** delete, do **not** regenerate.

## Scope
1. Per asset, owner picks ONE: provide from owner-held backup; OR authorize a
   logged, scoped regeneration via an existing safe pipeline (explicit
   approval, out of audit scope); OR authorize a scoped scrape (explicit
   approval). Recover `forex_factory_cache.csv` first (others may depend on it).
2. Record provenance + integrity (sha256, rows, date range, source, network
   on/off) in `06_GOVERNANCE_AND_COMPLIANCE/data_governance/`.
3. Provenance-audit `canonical_anchor_events.csv`: which connector
   (live BLS vs offline `stubs.py`), network used?, 2025/2026 rows? Decide
   keep/quarantine/regenerate-under-policy.
4. Re-run Phase E data + `research_lab/tests` checks; classify, never fake PASS.

## Hard rules
- No agent scrape/download/regeneration without explicit per-asset owner
  authorization (default = audit only). No 2025/2026 analysis. No
  backtest/strategy/F06-real/optimization/sweep/validation/holdout. No data
  deletion/overwrite. No ZIP. No main, no force push. Never synthesize/stub
  missing data to fake a green test.
